<%= form_with(model: wanted_item) do |form| %>
  <% if wanted_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wanted_item.errors.count, "error") %> prohibited this wanted_item from being saved:</h2>

      <ul>
        <% wanted_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :remark %>
    <%= form.text_area :remark %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
