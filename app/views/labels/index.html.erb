<h1>ラベル</h1>

<table>
  <thead>
    <tr>
      <th><%= t('activerecord.attributes.label.index.name') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @labels.each do |label| %>
      <tr>
        <td><%= label.name %></td>
        <td><%= link_to 'Show', label %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('activerecord.attributes.label.index.new_label'), new_label_path %>
