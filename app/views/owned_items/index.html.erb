<p id="notice"><%= notice %></p>

<h1>持っているグッズ</h1>

<table>
  <thead>
    <tr>
      <th>Quantity</th>
      <th>Remark</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @owned_items.each do |owned_item| %>
      <tr>
        <td><%= owned_item.quantity %></td>
        <td><%= owned_item.remark %></td>
      </tr>
    <% end %>
  </tbody>
</table>

