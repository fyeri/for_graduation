<div class="row justify-content-center">
  <div class="col-md-6 text-center-container">
    <h1><%= t('.account') %></h1>

    <p>
      <strong><%= t('.user_name') %></strong>
      <%= @user.name %>
    </p>
    <p>
      <strong><%= t('.email') %></strong>
      <%= @user.email %>
    </p>
    <%= link_to t('edit'), edit_user_registration_path, class: "btn btn-link" %>
  </div>
</div>